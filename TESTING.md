# 验收记录

## 0.2.4-alpha1

33项核心、57项浏览器流程通过；新增25项更新策略和33项原生版本文件/HTTPS模拟测试。覆盖不自动检测、无弹窗、较新/相同/较旧版本、手动下载、异常与过期响应、链接限制和响应大小限制。真实官网端点发布后复核，安卓真机浏览器下载/覆盖安装与小组件外观仍需确认。见 [UPDATE-0.2.4.md](UPDATE-0.2.4.md)。

## 0.2.3-alpha1

33项核心测试、52项浏览器测试通过。覆盖首页窄屏/大字、长按进度删除及各类取消、持久化失败、恢复与空课表。未做安卓真机触摸和厂商上岛验收。见 [UPDATE-0.2.3.md](UPDATE-0.2.3.md)。

## 0.2.2-alpha1

33 项核心测试、45 项浏览器流程通过。新增硕士同格双课/三课、独立周次与教师、多师班、合并行去重、损坏记录阻止部分导入，以及本科首页七天网格、教师留空与补填、个人完整课表优先、核对保存、TT2 导出测试。调课表内容单独保留，不自动改动已排课。

附件 PDF 的微信临时路径已失效，无法逐条对照原 PDF；仅使用用户截图结构和合成测试，不宣称已验收该学生全部课程。未登录真实教务账号。详见 [UPDATE-0.2.2.md](UPDATE-0.2.2.md)。

## 0.2.1-alpha1

本次40项浏览器流程及25项核心测试通过；Release构建0错误、8项提示。最终APK没有闹钟权限。最新改动与未验证范围见 [UPDATE-0.2.1.md](UPDATE-0.2.1.md)。

## 0.2.0-alpha1（历史记录）

25 项 Node、37 项浏览器流程、25项提醒计算断言、20 项原生时钟断言及14项域名断言通过；Release构建与lint完成（0错误）。安卓API35模拟器已完成安装、日期保存、示例课表导入、七列全览、桌面组件添加/今日显示/点击全览、当前课与课间边界检查。提醒入口、完整时段时间轴、自定义时间、交流弹窗、范围切换、非本周标记、缺失日期保护、详情编辑保存/取消、错误校验、按钮点击区域及四行详情已纳入测试。完整范围和未验证项见 [UPDATE-0.2.0.md](UPDATE-0.2.0.md) 和 [REMINDERS.md](REMINDERS.md)。

`tools/emulator-qa.mjs` 仅供本机 `emulator-5554 / SYUCT_Preview_API35` 合成样本测试；不能指向个人手机。`clock-test` 仅在示例5–6节课表及默认组件位置下适用，会临时修改模拟器时间并在结束时恢复。不在CI运行，也不包含在APK内。

`tools/emulator-layout.cjs /private/fixture.txt` 用于同一模拟器的本地 Debug 包布局验收，需要 `ANDROID_HOME` 和 Playwright。通过实际导入界面写入样本，检查竖屏全览、七天列边界、按周数量和详情字段。样本与截图只留在忽略的 `test-results/`；不上传 GitHub。Release 禁用 WebView 调试，测试后需覆盖安装 Release。

## 0.1.0-alpha1 历史记录

## 已执行

- Node 核心测试：19 项通过。
  - 本科匿名样本 40 个时间标记去重为 20 条安排，与参考字段逐节、逐周完全一致。
  - 硕士合成样本保留教学楼、教室；离散节次、周次、单双周保持精确，不填补空缺。
  - 不支持的周次、多个不同课表、错位表头、缺少节次、不可读框架均停止导入。
  - 总周默认 20；学期名称/日期可空；非周一与超出总周数禁止保存；TT2 往返保持字段。
- Chromium 界面和 DOM 测试：18 项通过。
  - 20 条安排进入核对流程，未确认不保存；编辑会撤销确认。
  - 保存采用修改后的内容；重载恢复；复制 TT2；无效设置不覆盖已存数据。
  - 390px 手机宽度无横向溢出，课程文本中的 HTML 不执行。
  - 本科采集不含表格外的模拟学号或密码；保留未排课提示。
  - 硕士常规表格与分离表头/表体采集可解析；首页摘要和空页面失败。
  - 同源框架课表可读；沙盒隔离框架停止导入；合并行不重复生成安排。
- Java 原生域名策略：14 个断言通过，拒绝 HTTP、仿冒子域、用户信息段、非标准端口与跨教务域名。
- Gradle Android 编译：Debug 和 Release 构建成功，已执行两种构建的静态检查。
- Release 签名验证通过（v2/v3），仅联网权限，最低 API 26 / target API 35，打包资源包含全部解析和界面模块。
- 静态检查没有错误；JavaScript 启用是教务与本地 UI 的必要能力，结合独立 WebView、无远程原生桥、CSP 和精确导航白名单防护。非国际化中文文案提示保留。

## 未验证（不能视为已通过）

- Android 真机安装、系统 WebView 兼容性、键盘/刘海/手势导航、进程回收后的界面恢复。
- 本科与硕士真实账号登录、验证码、认证域名、所有框架结构、当前学期真实 DOM。
- 同一硕士单元格含多门课程：0.2.2 已适配具有明确结束字段的布局；未知虚拟表格、动态按需加载行仍不宣称支持。
- 实际学校页面数据是否已经完全加载。DOM 中不存在的课程无法凭空恢复；必须与教务原表核对。
- 系统版本降级与不同厂商数据迁移行为。

## 手机验收清单

1. 安装 Release 签名内测 APK，检查首页打开无空白、字体和底部按钮正常。
2. 先导入一个已有课表码，保存后关闭 App 重开；检查离线查看和复制码可在小程序导入。
3. 本科分别测试首页和「学生个人课表」，核对每天课程数、单双周、教学楼及教室；首页教师应为空，个人课表优先采用完整信息。
4. 硕士进入「我的课程表」，确认星期一至日及所有节次已加载；读取并逐项核对。
5. 修改任意字段，确认勾选应自动取消；重新确认保存应采用新值。
6. 第一周日期选择周一，总周 20；检查本周和切周。名称、日期不填仍应可以保存。
7. 清除登录状态后教务应要求重新登录，但本机课表仍在。
8. 再次导入应提示替换；设置中恢复上一版应找回之前课表。

遇到问题只反馈页面路径（去除学号与会话参数）、错误提示及脱敏截图；不要公开密码、Cookie 或整份含个人信息的原始页面。
