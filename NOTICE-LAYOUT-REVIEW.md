# 提醒短文本与完整详情复查（0.2.16）

## 问题与处理

用户真机截图证明“剩余15分钟”太长；厂商浮窗没有按 BigText 正文展开，而将正文截成一行。不能用 AOSP 通知截图承诺小米同样排版。

- 通知普通标题和 Android 16 短文本统一为课程名前四个 Unicode 码点，去掉空白；不拼时间、倒计时或预览前缀。完整标题仍保留在 BigText 中。
- 系统摘要仅保留开课时间和查看详情提示；标准展开正文分行显示教师和教室。
- 增加非导出的只读详情 Activity，由通知自身不可变 PendingIntent 打开。不同课程用独立 URI，避免固定请求号导致点击旧通知打开另一门课。分项卡片支持自动换行、滚动、状态栏避让、大字体；缺失信息显示“未提供”。
- 小米浮窗可能仍只显示短摘要，用户点“查看详情”查看全部信息；不伪称已改变 OEM 浮窗高度。
- 删除分钟刷新实现和测试，移除对应 CI 命令；仅保留旧刷新 PendingIntent 取消兼容。旧回调不再修改或创建通知。
- 保留现有课前15分钟触发、精确定时授权、去重记录、结束和超时规则；不新增权限或服务。

## 已验证

- 核心33项、浏览器60项通过；正式 Release 构建和 lint 通过。
- API36隔离包：原生通知47项、实际预览按钮19项、详情页23项通过。
- 详情测试覆盖独立课程快照、四项信息、长文本换行、缺失值、非导出、返回、偏好不变、状态栏避让。另检查130%字体截图，长文字完整显示。
- 已授权权限入口11项通过；按钮仍允许进入系统设置且不会自动开提醒。
- 正式提醒30项通过。测试曾在 notify 后同一时刻读取 activeNotifications，系统尚未处理而失败；系统 dumpsys 证明通知实际存在，因此为测试添加有界等待，不改生产发送逻辑。
- 原签名证书 SHA256 保持 d0709cddeb8a90a879ba114d64b5633bbc0b78b6f4b141dfe1ab97852f6c285e。

## 边界

标准 Android 模拟器无法验证澎湃 OS 3 的实际岛宽度、图标着色及浮窗高度。四字载荷、完整详情入口已验证；真机最终视觉仍须用户确认。既有系统休眠、强停等送达限制没有因本次排版更新而消除。
