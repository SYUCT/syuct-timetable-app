# 0.2.8-alpha1：课前实时倒计时试验

沿用0.2.7提醒及小组件修复，新增可选的系统实时倒计时。2026-09-14用户确认先发布试用；上岛未验证限制保留，默认不开启。

## 使用与范围

设置 → 上课提醒 → 课前实时倒计时（试验），默认关闭。用户主动开启后，发送课前提醒时尝试请求系统提升；不支持或未允许的设备保留可关闭的普通通知。可手动预览3分钟效果，不开启正常提醒、不改动课表或已发送记录。

倒计时复用系统计时，不逐秒轮询，无新增前台服务。点击进入课表全览；上课时通过系统超时结束。通知提供结束操作和删除回调；课程发送记录避免重开后复活。编辑/删除课程会清理不再有效的已发通知。

## 实现

- 新增 `LiveCourseNotice`，复用 `CourseReminder` 渠道、普通通知、课表路由和持久去重。
- compileSdk升级36，minSdk26、targetSdk35保持，避免顺带改变大屏布局等行为。非Google Play发布。
- 使用官方 `android.requestPromotedOngoing` extras 请求，避免在基础API36调用仅API36.1公开的原生setter。
- 新增 `POST_PROMOTED_NOTIFICATIONS` 非运行时声明，不增加精确闹钟、悬浮窗、无障碍、前台服务权限。
- 是否允许提升与通知特征分别检查；不符合时回退普通通知，不把发送成功当成上岛成功。

## 验证与限制

33项核心、60项浏览器、40项提醒计算、20项原生时钟断言通过；完整Debug/Release及离线构建和lint通过（0错误、12项警告，新增旧target提示）。Android15和基础Android16镜像各通过13项普通通知降级/生命周期断言，基础Android16另外通过3项通知关闭断言；该镜像没有允许提升，不代表上岛成功。

已下载官方Android36.1 Google APIs ARM64 r04镜像，但本机模拟器37.1.11首次启动时QEMU线程挂起并以139退出，尚未进入功能验收。因此实时通知的实际提升、结束按钮、滑动关闭和提升后的开课移除均未完成运行验证；不得写成已上岛。测试源代码保留，可在支持设备上继续执行。

发布APK：`artifacts/SYUCT-Timetable-0.2.8-alpha1.apk`，versionCode10；SHA-256：`0b275830a154ecc2838b36a090fd64868ac1dfd741cad961390ba743eb8af3e6`。沿用原签名，正式包不含独立测试Activity。发布校验时修正了关于页残留的0.2.7显示，并重新构建签名。

厂商上岛外观、真实锁屏/滑动行为、后台省电延迟仍须真机验证。该功能改善展示，不解决系统调度延迟，不承诺所有Android16手机上岛。普通日历提醒不是官方推荐的实时通知用途；本试验限于用户主动关注的临近上课倒计时，不长期占用状态栏。

## 发布与回滚

按用户确认，将本次试验包发布到GitHub和官网，官网APK与版本文件必须同步更新为versionCode10及上述校验值；不修改小程序。优先在设置关闭试验开关恢复普通提醒。发现问题时优先发布更高版本号修复包；卸载会删除本机数据，降级前必须导出课表码。

参考：[Android Live Updates](https://developer.android.com/develop/ui/views/notifications/live-update)、[36.1新增原生调用](https://developer.android.com/sdk/api_diff/36.1/changes/android.app.Notification.Builder)。
