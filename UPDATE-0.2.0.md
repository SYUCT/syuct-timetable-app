# 0.2.0-alpha1 更新与验收

## 四项改动

1. 全览保持竖屏，七天等宽同时显示；单元格按可用高度拉伸，最小72px/节，字号增大。顶部按钮统一为「切换」，切换按周与全学期安排，下方注明范围。非本周课程增加文字标记及虚线边框，日期未设置时不猜测。卡片采用课程配色和简化地点；详情可修改本条安排，保存后同步本机课表与组件，取消不改变数据。冲突课程并排，首页区域保持两行。
2. 首页「设置第一周」打开日期弹窗，保存时校验周一，无需跳转设置页。保存、恢复课表同步更新小组件。
3. Android 原生桌面小组件：显示当日课程，可纵向滚动；点击课程或标题打开 App 当前周竖屏全览。移除刷新按钮与底部更新文字，采用校徽、圆角课程卡和完整上课时段。设置页提供请求添加按钮，也可长按桌面添加。需先设置开学日期才判断“今日”教学周。
4. App 和小组件增加当前课标记。统一使用北京时间，遵守单双周、学期和节次范围；上课开始包含、下课时刻不包含，课间不标记。第1–10节时间源于现有小程序；第11–12节未提供，不编造，设置页可填全部节次时间。

模拟器检查另发现并修复两处遮挡：WebView 改由父容器避开系统状态栏/手势条/键盘；全览固定节次列限制在表格层级内，不覆盖底部导航。

时间轴以双节显示完整起止时间，如08:00–09:50、13:30–15:20，课程仍按实际单节位置排列；自定义时间同步生效，未知时段显示待设置。设置页新增「项目与交流」，包含 GitHub 组织首页、新生交流群1170264357（复制）和校园指南网站（外部浏览器打开）。

## 更新机制与权限

新增每次上课前15分钟提醒及设置开关，使用系统默认通知声音。首次开启时申请通知与准时提醒权限；不强制绕过静音或勿扰。计算和权限说明见 [REMINDERS.md](REMINDERS.md)。

- 小组件数据只读应用私有课表，不读取教务账号。
- RECEIVE_BOOT_COMPLETED 用于重启后恢复已添加组件及课程提醒，不是后台常驻服务。
- 保存/恢复/打开 App 立即刷新组件并重新预约课程提醒；组件系统约30分钟周期更新，并预约下一上课、下课或午夜边界的非唤醒刷新，有权限时使用精确定时。
- 课程提醒另申请 POST_NOTIFICATIONS 与 SCHEDULE_EXACT_ALARM。用户可拒绝或关闭；未允许准时提醒或受系统省电限制时可能延迟，不承诺所有设备后台秒级送达。
- 自定义节次时间只在 App 本地保存，SYUCT-TT2 协议不承载作息时间表。导入课表码保留本机作息，换设备需另设。

## 本次已验证

- 25 项 Node 测试通过：含原19项与全览分列、冲突排布、上下课边界、单双周、北京时间、未知节次、自定义时间校验。
- 37 项 Chromium 页面与 DOM 测试通过，包含提醒设置入口、完整时段及自定义时间轴、交流弹窗三入口、日期直接保存、七天同时可见、单行工具栏、范围切换与提示、非本周标记及缺失日期保护、详情编辑/取消/校验、64×44按钮点击区域、四行带标签详情、退出恢复导航、当前课、组件入口及原导入回归。
- 20 项 Java 时钟断言、14 项域名策略断言通过。
- 新增25项原生提醒计算断言通过，覆盖去重、单双周、起止周、同时开课、过期取消、修改时间和未知时间等。
- Gradle `assembleRelease lintRelease` 成功：0 错误、8 项提示（提醒去重记录同步写入、JavaScript 必需能力、较新组件尺寸属性在旧系统忽略、中文文案国际化、按产品要求固定竖屏、彩色校徽未提供单色主题图标）。
- Release APK 与0.1版同证书，v2/v3签名验证通过，可覆盖升级。
- Android 15 / API35 ARM64 Pixel 6a 模拟器：签名包安装、覆盖升级、冷启动显示、首页日期弹窗选择与保存、TT2示例核对保存、横向全览、小组件添加、读取今日课与楼号、点击课程进入当前周全览均完成实际检查。
- 原生小组件时钟测试：14:40 显示「当前课」，14:25课间及15:20下课边界不显示；测试后恢复模拟器时钟与自动时间设置。
- 本次本地课表码样本：19 条安排逐字段保存一致，总周数20。第2/10/16/17周显示数量与课程周次筛选一致；长课程详情、完整教室/教师、返回单日页、首页日期弹窗通过。真实样本与截图不进入代码仓库或安装包。

## 未验证与限制

- 未验证实体手机和各厂商桌面、组件多行滚动/尺寸变化、长期待机及重启后的刷新；模拟器结果不代表所有手机。
- 未验证本科/硕士真实账号登录与最新教务 DOM。请使用本人账号、完整个人课表进行核对；不得将示例测试视为学校实测。
- 未验证旧安卓版本的系统 WebView 兼容性。本版本仍为内测版。

## 发布与回滚

同步独立 App 仓库，提交代码后创建 `v0.2.0-alpha1` 预发布，上传同证书签名 APK。网站首页另增加下载入口，发布时须同时提交 `index.html` 和同证书 APK `downloads/SYUCT-Timetable.apk`，不能只发布按钮。小程序不变。不要上传私钥、模拟器数据或真实个人课表。

沿用本机现有签名目录可覆盖升级，不必卸载旧版。若卸载应用会丢失本机课表，升级前仍建议复制课表码备份。
