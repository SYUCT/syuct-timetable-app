# 0.2.11-alpha1 · 通知图标与课程短标题

版本号 13，沿用现有签名，可覆盖安装保留本机课表。发布渠道为 GitHub Releases 与官网。

## 改动

- 小米/Redmi/POCO（Android 16、focus protocol >= 3）增加官方左右分区参数：`bigIslandArea.imageTextInfoLeft` 是彩色校徽与课程前三字，`bigIslandArea.textInfo` 是独立开课时间；不是用课程名替换时间。大/小岛及展开卡片使用 `miui.focus.pics` 引用原图，保持深浅模式同一彩色校徽。展开保留完整课程名称、地点与预览标识；结束动作引用原 PendingIntent。
- 官方模板库第77页明确此图文+文本模板超长会缩小/裁切、不滚动，因此使用三字静态名称，不以循环重发通知模拟跑马灯。小米没有焦点模板资格时保留通用芯片的开课时间，不能宣称单槽芯片可以强行显示两个区域。
- 其他通用岛的 shortCriticalText 使用真实课程名称前三个 Unicode 字符；空名称显示「待上课」。预览也用课程名称，不截取「预览」二字。不循环刷通知、不添加滚动服务。
- 时间仍保留在 when / 系统倒计时和展开内容中。标准 Android 芯片只有一个短文本槽；不能承诺在课程名旁同时显示时间或固定在左侧。
- 小图标不再把校徽的深色画面转成白色反相蒙版，而改用正向亮度蒙版。白底彩色原图继续用于大图及 vivo 专用模板。**通用系统小图标强制染色，因此这一后备方案不是完整彩色校徽；未声称已解决用户手机的所有图标位置。**
- Android 16 起，仅 vivo/iQOO 且用户已开启实时倒计时时附带本地 `notification.superx.*` 实验参数：左侧三字课程、右侧开课时间、原图校徽。普通手机不发送这些参数。
- 使用文档中的日程场景 `METTING`，不伪装火车、导航、媒体或挂号；不调用隐藏的场景注册方法，不添加厂商推送 SDK、服务器、常驻服务、悬浮窗权限或通知监听权限。
- 复用同一条通知与原超时。结束按钮、关闭实时功能、删除课程时，在已有 vivo 消息上发 operation=2 后取消；没有已有消息时不重发。系统超时后的厂商岛消失行为需要真机确认。
- 准时提醒预约、发送记录、防重复、课表导入导出逻辑不变。

## 资料与限制

- [Android Live Updates](https://developer.android.com/develop/ui/views/notifications/live-update)：系统控制图标、文本宽度与提升资格。
- [vivo 本地原子岛接入说明（阿里云 EMAS）](https://help.aliyun.com/zh/document_detail/3030718.html)：支持本地 Notification extras，正式接入需申请原子岛权限；参数不是绕过授权的开关。中文文档将日程场景拼作 `METTING`，与英文文档 `MEETING` 不一致，正式接入时须由 vivo 确认。
- [OriginOS Toolkit 协议说明](https://github.com/cameleonnbss/originos-toolkit/blob/main/docs/ORIGINISLAND.md)：核对 capsule/island 本地字段。这里只参考协议字段，未引入该项目的通知转发、系统修改或隐藏 API。其 TRAIN 场景与课表不符，没有采用。

vivo 原子岛实际显示、资格审核、系统配色、自动到时撤岛与滑动关闭尚未真机验证。用户已确认截图来自小米，具体机型/系统版本未知。小米左右模板实际显示、焦点通知资格、原图和深浅模式效果未真机验证，不能把模拟器普通通知当作上岛验收。

小米依据：[官方开发指南](https://dev.mi.com/xiaomihyperos/documentation/detail?pId=2131) 及其链接的《小米超级岛模板库20260129》：第77页左右组合、第95–96页左图文、第104–105页右文本、第39页彩色识别图形。岛超时单位秒、焦点卡片超时单位分钟，并保留毫秒级原生 timeout；不设置未授权时过滤通知，不设置重新上岛。使用标准 cancel 清除。没有接入 MiPush、没有冒充媒体播放。

## 本地验证

- 课表核心 33 项通过；浏览器 60 项通过。
- 原生时钟 20 项、提醒计划 40 项通过。
- API 35 独立通知包 52 项通过：品牌色、正向蒙版、原图引用、三字截取/空值/Unicode、vivo 参数布局与品牌隔离、小米同时包含左课程/右时间、日夜图片引用、结束动作、JSON 转义、通知跨进程序列化、不同单位超时、默认关闭、普通回退、取消、自动超时、不修改发送记录。
- Android release/debug 编译通过；lint 无错误，12 条既有警告。
- 未验证：真实 Android 16 提升后的芯片布局、用户机型染色、vivo 专用模板显示和结束。APK 签名和最终版本以构建记录为准。

## 发布/回退

经用户确认，作为 alpha 版本推送代码、建立 v0.2.11-alpha1 Release，并同批更新官网 APK 和 app-version.json（versionCode 13）。上述真机未验证项仍保留，不将本次发布视为厂商上岛验收。

不建议卸载降级，以免丢失本机课表。需要回退时，从上一版本源代码重新构建为更高 versionCode，使用原签名发布覆盖更新；或者先关闭实时倒计时，保留普通课前提醒。
