# 0.2.15-alpha1

## 更新

- 修复“闹钟和提醒权限”按钮在已授权或关闭准时提醒时消失的问题。现在固定显示入口及状态，允许随时查看系统权限；进入权限页不会自动打开提醒开关。
- 小米岛的短文本及普通通知标题统一使用剩余分钟数，不再回退为课程名。展开保留课程、时间、教师、教室；摘要也包含完整信息，缺失字段明确标注。
- 显示更新仅在课前倒计时期间静默执行，不唤醒休眠手机，不新增权限/常驻服务，不接入厂商 App ID。省电期间分钟更新仍可能延迟；其他系统保留原生计时器。
- 精简未使用的小米/vivo专用模板、权限查询和旧通知图标，保留旧通知取消兼容。课表导入、数据格式和正式提醒调度规则不变。

## 验证与边界

核心、浏览器及原生通知回归详见 [NOTICE-REVIEW.md](NOTICE-REVIEW.md)。权限入口追加隔离 Android 测试，覆盖准时提醒关闭/开启、系统拒绝/允许四种组合，以及按钮目标 Intent 与用户开关不被修改。

Android 标准模拟器不等于澎湃 OS 3 真机。实际岛渲染、厂商卡片高度限制和省电刷新及时性仍需真机确认。没有保证每秒刷新岛文字。

## 上线与回退

版本名0.2.15-alpha1、versionCode17，沿用原签名。请覆盖安装，不要卸载，以免删除本地课表。

发布先上传 GitHub 预发布 APK，再更新官网 `downloads/SYUCT-Timetable.apk` 与 `app-version.json`（版本、SHA-256一致），待部署完成后校验线上文件。

若需回退分发，恢复0.2.14安装包与对应版本清单；已安装17的用户不能直接无损覆盖降级，应将回退代码用更高versionCode和原签名发布。不要要求用户卸载；保留课表码备份。
