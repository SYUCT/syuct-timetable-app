# 0.2.7-alpha1：提醒与小组件反馈修复

## 原因与修改

旧逻辑每次重开 App 都取消预约，然后只找未来的提醒时间。若已经进入课前15分钟、系统尚未送达，当前课程会被跳过。现在先补发窗口内未发送项，再预约下一批；同一未来预约保留，已发送项持久去重，上课后不补发。

提醒设置新增下次计划时间及测试通知，显示通知关闭和调度失败等原因。测试通知不改动课表、提醒开关或发送记录。

原小组件入口只判断桌面是否接受请求，无法证明添加成功。现在使用带请求标识的成功回调，并检查是否新增小组件；未确认、桌面不支持、异常分别提示手动添加。不能控制桌面换页。

## 验证

- 40 项提醒计算、33 项核心、60 项浏览器测试通过。
- API 35 独立测试包：21 项通知允许路径、4 项权限关闭路径通过。
- 实际验证创建通知、窗口内补发、重开防重复、关闭后不复活、开课不补发、开关关闭清除预约。
- Pixel Launcher 实际添加小组件并收到成功回调；未确认和过期回调等逻辑通过。
- Release 构建成功，lint 0 错误、11 项警告。测试 Activity 仅放在独立测试包，正式包不包含。
- versionCode 9，versionName 0.2.7-alpha1。沿用现有签名，权限仍仅联网、开机完成广播、通知。

## 未验证

真实手机长时间待机、Doze、厂商后台限制、重启后实际送达延迟、声音听感及满桌面行为。模拟器注入时间的测试不代表15分钟准时送达。无精确闹钟、悬浮窗、无障碍或常驻服务权限。

## 交付与发布

本次生成 `artifacts/SYUCT-Timetable-0.2.7-alpha1.apk`，不自动发布。先导出课表码备份，覆盖安装验证提醒及小组件。确认后提交源码，创建对应 Release，上传同一签名 APK，再更新官网安装包和版本文件（versionCode、下载地址、校验值须一致）。

回滚优先发布更高 versionCode 的修正版。Android 通常不允许直接降级；若需卸载重装旧版，必须先备份课表码，卸载会删除本机数据。
