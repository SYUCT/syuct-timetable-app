# 通知兼容与精简审查（0.2.15）

## 问题与证据

- 用户的 0.2.14 截图中，展开计时器已经运行到 14:41，说明不是简单的旧包未更新。
- `setShortCriticalText(null)` 留空后，岛显示的“预览·自然辩证…”恰好来自普通通知标题，而不是倒计时。AOSP 卡片计时通过不能证明澎湃 OS 的岛使用同一字段。
- 教师、教室原先在 BigText 的第二、三行，普通正文未包含这些字段。截图仅显示第一行；符合厂商摘要按首段处理的表现，但没有小米系统日志，不能宣称已确认厂商内部实现。

## 修改

- 仅对小米/Redmi/POCO 的可实时显示通知，显式设置短文本和普通标题为“剩余15分钟”等；完整课程名仍在展开标题。
- 小米分钟文本最多按 15 个边界更新。使用非唤醒 RTC 预约，不启动服务、不增加权限、不每秒轮询。已开启并授权准时提醒时复用其精确定时能力；否则使用普通定时，可能延迟。休眠期间不为显示更新唤醒手机。
- 更新只读取已经存在的通知记录，根据实际开课时间重算剩余值，只更新同一条通知，设置 OnlyAlertOnce；不修改课程或已发送记录。关闭、删除或到时后不重新创建通知。
- 其他系统仍使用原生倒计时。普通通知降级路径保留原课程标题。
- 普通正文和展开正文均包含时间、教师、教室；改为一个可自动折行的段落，避免信息藏在后续换行中。缺失字段继续明确显示“未提供”。

## 精简与边界

- 删除实际路径已不调用的小米模板构造、焦点权限查询、vivo 模板生成；不再把废弃模板的单元测试当作生产功能验证。
- 保留小米品牌判断和图标兼容提示；将旧版通知取消兼容集中在 `NoticeCompat`，避免影响旧通知结束。
- 删除无引用的旧通知图标、三字课程名辅助函数、未使用的构造参数；正式提醒与预览共用发送及显示更新入口。
- 未调整导入解析、TT2、保存格式、课前15分钟计算、准时提醒唤醒策略及发送去重键。网站仅更新安装包与版本清单。
- 另修复权限按钮被隐藏的问题：Android 12及以上固定显示“闹钟和提醒权限”，已允许/去授权随系统状态变化；已授权也可打开查看。打开页面不自动开启准时提醒，旧系统明确显示无需单独授权。

## 验证

- 核心解析 33 项、浏览器流程 60 项通过。
- URL 策略14项、时间计算20项、提醒规划44项、手动更新策略25项通过。
- 剩余分钟边界10414个断言通过，15分钟内最多15个更新边界。
- API36隔离包：通知显示/更新/取消/过期50项通过（包含真实非唤醒定时回调）；拒绝通知权限28项通过；实际预览按钮19项、按钮拒绝权限6项通过。
- 正式提醒原生回归30项、通知拒绝路径8项通过。
- 权限入口追加22项通过（系统未允许/已允许各11项，均覆盖准时提醒关闭/开启）；发布前核心33项、浏览器60项及预览按钮19项再次通过。
- 实际仓库 Release 构建、lint 通过（0错误，11条既有警告，已消除无引用通知图标警告）。测试包与正式包隔离，不改个人手机或真实课表。
- 查看合成通知截图，确认完整课程名、开课时间、教师、教室均可见；不是小米岛验收。计入新增兼容代码后，正式源码及资源净减少61行。

## 未验证

澎湃 OS 3 真机岛最终渲染、厂商对通知正文的额外高度限制，以及锁屏/省电/强杀下的分钟刷新及时性，不能由 AOSP 模拟器证明。没有新增厂商 App ID 接入、伪装通知场景或后台保活服务。
