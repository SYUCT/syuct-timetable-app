# 0.2.14-alpha1：15 分钟系统倒计时

## 本次改动

- 不接入小米 App ID 或专用岛模板。移除运行时的厂商模板选择及小米焦点权限查询；统一使用已存在的 Android 系统实时通知入口。
- 收起时不再传递课程名或绝对开课时间：清空 shortCriticalText，使用未来 when、showWhen、系统 Chronometer 与 countdown。由系统走时，不做每秒后台刷新。
- 正式提醒仍在开课前 15 分钟触发，结束时间始终为实际开课时间；迟到发送只显示剩余时间，不重新启动 15 分钟。预览也改为 15 分钟，保留即时反馈、后台发送与接收确认。
- 展开内容为完整课程名、开课时间、授课教师、上课教室。教师字段从本机课表传递到提醒和预览，缺失信息显示“未提供”，不猜测。
- 保留原提醒去重键，升级或补充教师后不重复提醒。结束动作、系统超时、取消后不重发及隐私隐藏保留。
- 不新增权限或常驻服务；厂商旧模板辅助代码仅保留作历史测试，不参与本版通知构建。

## 验证

- 核心 33 项、浏览器 60 项、原生时钟 20 项、提醒计算 44 项通过。
- API 36 独立通知探针 79 项通过，覆盖系统倒计时字段、无固定文字、15 分钟期限、迟到时剩余期限、取消和真实短时到期移除。
- 正式提醒探针 30 项通过，通知关闭路径 8 项通过。已验证本机课表教师字段、实际通知四项信息、旧数据缺教师以及去重记录兼容。
- 预览按钮 19 项、拒绝通知权限 6 项通过，预览不改变正式课表、提醒开关或发送记录。
- 可视探针调用同一倒计时构建函数，两次截图确认系统计时由 14:48 减少至 12:44，展开四项信息完整；这是标准通知走时验证，不是小米岛实测。
- 官网回归 108 项通过、1 项可选本地小程序联测跳过；原转换、OCR、研究生 PDF 测试通过，版本清单与 APK 哈希匹配。
- Release、Debug 及离线编译通过，lint 0 错误、12 项既有警告。
- 原签名验证通过，versionCode 16；APK SHA256：`6860baf51dd1b1c69294babf85d77a01fa263abd36ed14d0bc1878bb7de5d13b`。

## 边界

- 标准 API 36 模拟器未允许实时通知提升，普通通知及构建参数测试不等于小米超级岛已验收。
- 首次测试错误地假定所有 API 36 镜像都允许相同的提升特征；旧镜像的判定与较新系统不同。改为比较移除固定文字前后资格不变，并保留实际权限与降级测试，没有为通过测试修改应用的权限判断。
- 澎湃 OS 3 岛内究竟显示分秒还是分钟、图标染色和布局由系统决定，仍待真机确认。不承诺固定的秒数样式。
- 未做整段 15 分钟待机/Doze 或厂商杀后台验收；保持原有可选“准时提醒”。

## 发布和回退

同步 GitHub Release、官网 APK 与更新清单，直接使用原签名覆盖安装，不卸载旧版。
如需停止本版新增展示，可关闭“课前实时倒计时”，普通上课提醒仍可独立使用。需要版本回退时应将旧逻辑打包为更高 versionCode 的修复版；不要求学生卸载降级，以免丢失本机课表。

## 接口依据

[Android Live Updates 状态栏计时说明](https://developer.android.com/develop/ui/views/notifications/live-update)：无固定 shortCriticalText 时，通过未来 when 和系统 Chronometer 表达倒计时；厂商仍可追加显示限制。
