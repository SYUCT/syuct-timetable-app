# 0.2.2-alpha1 导入修复

- 硕士同一单元格可拆分两门或多门课程；按学校的「电话：」课程结束字段确认边界，不按课程名猜测。每门分别保留周次、节次、教师和地点；任一门数据损坏时停止整次导入。
- 「1班（一班多师）」等班级信息不再写成教师；周次后的教师备注用于该安排并提示核对，没有明确教师时留空。教师姓「单」不会误判成单周。
- 本科首页按星期行与时间字段读取，要求完整七天网格。教师信息留空，支持核对时补填。完整个人课表与首页同时存在时优先完整课表，不混合叠加。
- 调课信息单独保留并提示核对，不擅自覆盖常规课表。缺失地点保留为空，已有保存确认与 TT2 协议不变。
- 通知仍为系统普通通知，可划掉，到上课时间自动移除。未接入系统岛形通知，不新增任何权限；厂商审核要求见 REMINDERS.md。

## 验证与限制

33 项核心测试、45 项浏览器流程通过，覆盖 DOM 采集、字段适配、核对、修改、保存和 TT2 导出；未确认前不覆盖本机数据。

原生回归：14 项域名规则、20 项时钟规则、25 项提醒计算通过。Release 构建通过，静态检查 0 错误、8 项已有提示。最终 APK 仍仅声明 INTERNET、RECEIVE_BOOT_COMPLETED、POST_NOTIFICATIONS；v2/v3 签名验证通过，打包解析器与已测试源码校验值一致。

APK SHA-256：`986f9709bb113553add114efeff712d208a78b451786f8329b8e068f841e1ebf`。

原 PDF 的微信临时路径失效，已请求重新提供。不能据此声明该 PDF 全部课程均已逐条核验；截图对应结构使用合成数据复现。真实账号登录、各厂商 WebView 的换行方式、按需加载的隐藏行仍需实机验收。本轮模拟器未运行，未执行新版原生界面安装验收。

同签名版本号升至 4，可覆盖安装；先导出课表码备份。安装包不含测试课表、用户截图、PDF、账号信息或签名密钥。
