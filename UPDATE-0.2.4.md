# 化大课表 0.2.4-alpha1

## 改动

- 设置 → 版本更新 → 检测更新。点击后才联网，启动、进入设置、恢复前台均不自动检测。
- 页面内显示正在检测、已是最新、发现新版或失败提示，不使用弹窗和通知。
- 官网静态版本文件：`https://www.syuct.top/downloads/app-version.json`，无需自建服务器。比较 Android versionCode，低于或等于本机版本不提示下载。
- 新版需再次点击下载按钮才交给系统浏览器；不自动下载/安装，不请求安装权限。下载地址限定官网固定 APK 路径，不信任远程 intent 或任意第三方链接。
- 检测不读取/上传课表、教务账号、Cookie或设备标识。连接与读取超时，响应最多32KiB，禁用自动重定向与本地缓存；使用普通HTTPS证书校验。普通网络请求仍会使官网获知连接IP。
- 仅手动请求进行中的结果可更新页面，超时后旧结果不覆盖重试结果。
- 小组件右上角校徽36dp → 44dp，增加8dp间距，保留原校徽和布局。
- 上岛暂不改动。权限仍仅网络、开机广播、通知三项。

## 验证

APK SHA256：`ed26fe87bd76686a6d41de40c6b8dad4a594c1f64a44d4328c74fcb2d8a88772`。

- 33项核心测试、57项浏览器流程；截图已检查。
- 25项原生更新策略、33项原生更新文件/模拟HTTPS测试；14项URL策略、20项时钟、25项提醒计算仍通过。
- 网站版本文件结构、安装包哈希与内嵌版本、缓存规则已加入自动检查。网站原HTML/TSV、PDF、OCR与统计测试仍通过。
- Android Release构建及签名检查通过，静态检查0错误、8项原有警告。
- 浏览器保存/原生接口和网络单元测试使用模拟实现；真机检测、浏览器下载覆盖安装及各桌面小组件外观仍需验收。

## 发布与回滚

1. 使用原签名，versionCode为6；同时发布网站APK和app-version.json，核对SHA256，不混用旧包和新版本号。
2. 发布后读取官网JSON、下载APK核验；App中点击检测确认当前已是最新版本。下次发布递增versionCode并同步JSON。
3. 0.2.3及以前没有检测入口，需先手动覆盖安装0.2.4。不要卸载，以免丢失课表。
4. 暂停更新提示可把网站APK和JSON一起回退；已装新版不提示降级。需要修复回滚代码时用更高versionCode重新构建发布。
