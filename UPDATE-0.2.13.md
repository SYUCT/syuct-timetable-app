# 0.2.13-alpha1：恢复通用实时提醒

## 已确认的回归

0.2.12 在小米协议 >=3、焦点权限查询为允许时，提前返回专用模板通知，并主动清除 Android 通用提升请求、ongoing 状态和短文字。用户截图证实真机已进入“允许”分支，但并未显示预览岛。权限允许不能证明专用模板的字段与本机系统适配成功；之前测试反而断言通用提升被关闭，是验证目标选错了。

## 修正

- 小米不再自动发送未验证的专用模板，统一走通用实时通知。保留 Android 提升请求、ongoing、课程简称＋开课时间、系统倒计时、结束按钮与超时；系统没有给予通用提升权限时仍退回普通通知。
- 焦点权限查询仅用于状态说明，绝不决定关闭通用倒计时；仍不承诺系统把短文字分为左右两区。
- 预览点击立即显示“正在发送”，同时禁用重复点击。通知构建、发送及接收检查移至临时工作线程，不阻塞设置页主线程。系统调用较慢时5秒后显示等待提示。
- 短时检查当前 App 的活动通知（最多8次、间隔150毫秒），并匹配本次请求标识，不把上一条预览误认成本次成功。系统接收记录仅证明接收，不代表 OEM 已画出灵动岛。
- 权限关闭、实时功能未开启、异常、未确认接收均有明确结果。原截图的“反馈文字没有变化”尚无真机日志，不能断言已复现其唯一原因；本次补齐点击与返回过程的可见反馈。
- 用户再次主动点击预览时允许重新提醒，但沿用同一通知 ID 替换旧预览，不堆积。关闭实时功能时也取消普通回退的预览通知。
- 对话框关闭后忽略旧工作结果，恢复按钮状态；不新增常驻服务、权限、账户信息采集，不改正式提醒预约与发送去重记录。
- 校徽原图及小米兼容提示保持0.2.12实现，本轮不再改图标参数。系统是否仍染色待真机确认，不能把本次功能恢复当作彩色图标问题已解决。

## 验证

- 课表核心33项、浏览器60项、时钟20项、提醒计划40项通过。
- API36 NativeLiveProbe 75项通过，包含修正后的通用提升请求/持续标记/短文字断言、结束和超时检查。
- PreviewButtonProbe 实际 ReminderSettings 按钮回调测试19项通过；撤销系统通知权限后6项通过。覆盖即时忙碌反馈、功能关闭、权限拒绝、实际发送记录、重复预览替换和关闭撤销。
- 通过 UI 层读取可见按钮位置并实际触摸点击，反馈显示“系统已接收3分钟预览……当前为普通通知，系统未允许实时提升显示”。这台标准模拟器没有允许提升，不能将普通通知测试误报为真机上岛成功。
- Release/debug构建成功，最终 lint 0错误、12条既有警告。原签名验证通过，versionCode 15。
- 最终 APK SHA256：`a0edfc6c66452be765129005f65ccc280bb13786021a81f4fd454faee8ed0a7d`。
- 未验证：澎湃 OS 3 真机恢复上岛、彩色图标、短文字是否完整。用户真机“点击不更新反馈”的唯一原因尚无日志证实，不能把新增反馈流程说成已精准复现所有症状。

## 交付

经用户确认，发布 versionCode 15，同步 GitHub Release、官网 APK 及更新检测。使用原签名覆盖安装，不要卸载旧版，以免丢失课表。上述真机待验证项继续保留，不把发布视作小米视觉验收。
